// AI Collab — Content Script
// Lightweight script injected into all pages for page context extraction
// The actual extraction logic is in background.js via chrome.scripting.executeScript
// This script exists primarily for future extensibility (e.g., highlight-to-ask)

(() => {
    // Listen for context menu or highlight-based triggers (future feature)
    // For now, this is a placeholder that confirms the extension is active on the page
})();
